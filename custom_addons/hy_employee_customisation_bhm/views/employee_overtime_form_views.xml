<odoo>
    <record id="employee_overtime_form_view_tree" model="ir.ui.view">
        <field name="name">employee.overtime.form.view.tree</field>
        <field name="model">employee.overtime.form</field>
        <field name="arch" type="xml">
            <tree string="Employee overtime Form">
                <field name="name" />
                <field name="employee_id" />
                <field name="requested_date" />
                <field name="company_id" groups="base.group_multi_company" />
                <field name="state" decoration-success="state == 'submitted'" widget="badge" />
            </tree>
        </field>
    </record>

    <record id="employee_overtime_form_view_form" model="ir.ui.view">
        <field name="name">employee.overtime.form.view.form</field>
        <field name="model">employee.overtime.form</field>
        <field name="arch" type="xml">
            <form string="Employee overtime Form">
                <header>
                    <button name="action_confirm" data-hotkey="q" string="Submit" type="object"
                        class="oe_highlight" invisible="state != 'draft'" />
                    <button name="action_approve" string="Approve" type="object"
                        class="oe_highlight"
                        invisible="state != 'submitted' or user_id != uid" />
                    <field name="state" widget="statusbar" />
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1>
                            <field name="name" placeholder="e.g. Employee overtime" />
                        </h1>
                    </div>
                    <group>
                        <group>
                            <field name="user_id" invisible="1" />
                            <field name="employee_id" readonly="state != 'draft'" />
                            <field name="signature" widget="signature" />
                            <field name="bank_name" readonly="state != 'draft'" />
                            <field name="iban" readonly="state != 'draft'" />
                        </group>
                        <group>
                            <field name="requested_date" readonly="state != 'draft'" />
                            <field name="create_uid" />
                            <field name="company_id" readonly="state != 'draft'"
                                groups="base.group_multi_company" />
                        </group>
                    </group>
                    <group>
                        <field name="note" readonly="state != 'draft'" />
                    </group>
                    <notebook>
                        <page name="overtime_details" string="Overtime Details">
                            <field name="line_ids" nolabel="1" readonly="state != 'draft'">
                                <tree editable="bottom">
                                    <field name="form_id" column_invisible="1" />
                                    <field name="name" />
                                    <field name="total_hours" />
                                    <field name="hourly_rate" />
                                    <field name="amount" />
                                </tree>
                            </field>
                        </page>
                    </notebook>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids" widget="mail_followers" />
                    <field name="message_ids" widget="mail_thread" />
                    <field name="activity_ids" widget="mail_activity" />
                </div>
            </form>
        </field>
    </record>

    <record id="employee_overtime_form_action" model="ir.actions.act_window">
        <field name="name">Employee Overtime Form</field>
        <field name="res_model">employee.overtime.form</field>
        <field name="view_mode">tree,form</field>
    </record>

    <menuitem
        id="employee_overtime_form_menu"
        name="Employee Overtime Form"
        parent="hr.menu_hr_root"
        action="employee_overtime_form_action"
        sequence="40" />
</odoo>